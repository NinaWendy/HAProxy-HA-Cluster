[Unit]
Description=Keepalived service for VRRP and LVS
After=network.target

[Service]
Type=forking
ExecStart=/usr/local/sbin/keepalived -D
ExecReload=/bin/kill -HUP $MAINPID
PIDFile=/var/run/keepalived.pid
Restart=on-failure

[Install]
WantedBy=multi-user.target
